import { AccountValues } from "@/types";
import React from "react";

export default function AccountTables({
  accountValues,
}: {
  accountValues: AccountValues;
}) {
  return <div>AccountTables</div>;
}
